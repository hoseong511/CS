#include <stdio.h>

int main(void) {
  // int a;
  // scanf("%d", &a);
  // printf("입력한 숫자는 %d입니다.\n", a); 
  // double b;
  // scanf("%lf", &b);
  // printf("%.2f\n", b);
  int c, d;
  scanf("%d %d", &c, &d);
  printf("%d %d", d, c);
  // scanf("%1d%1d%1d",) 붙어있는 입력값을 1자리씩 데이터를 할당할 수 있다.
  return 0;

}